node_modules/
dist/
out/
.vscode-test/

# Local secrets: the class token, the database URL. Never commit these.
.env
.Renviron

# The rendered dashboard embeds student usernames and question text. This is a
# public repo — keep the output local.
dashboard/dashboard.html
dashboard/dashboard_files/
dashboard/*_files/
